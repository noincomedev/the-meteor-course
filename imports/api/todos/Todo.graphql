type Todo {
  _id: ID
  owner: ID
  createAt: String
  taskName: String
  done: Boolean
}

extend type Query {
  allTodos: [Todo]
  userTodos: [Todo]
}

type Mutation {
  createTodo(taskName: String!): Todo
  closeTodo(_id: ID!): Todo
}
